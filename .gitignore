# ─────────────────────────────────────────────────────────────────────────────
# Build artifacts & scratch — removable on any OS with:  java tools/Clean.java
# (preview with:  java tools/Clean.java --dry-run). See README → "Cleaning up".
# ─────────────────────────────────────────────────────────────────────────────

# --- compiled output ------------------------------------------------------
bin/
build/

# --- packaged runnable jar (rebuild with the commands in README) ----------
JBlackJack.jar

# --- build / run scratch --------------------------------------------------
sources.txt
.sources.tmp
*.log
*.out
*.err

# --- test / verify scratch ------------------------------------------------
jbj_*
accounts.bak
CurSize.*
PathChk.*
Res.*

# --- macOS / editor artifacts ---------------------------------------------
.DS_Store
.AppleDouble
.LSOverride
._*